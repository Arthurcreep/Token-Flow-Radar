Make sure backend/src/app.js has:

const tokenImportRoutes = require('./modules/token-import/tokenImport.routes');

and:

app.use('/api/tokens', tokenImportRoutes);
app.use('/api/tokens', tokenRoutes);

tokenImportRoutes must be registered before tokenRoutes if tokenRoutes has broad dynamic routes.
