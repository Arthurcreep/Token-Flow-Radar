Patch frontend/src/pages/TokenDetailPage.jsx after switching to unified endpoint.

Replace old shape references:

1. Replace:

const summary = profile?.cexFlows?.summary || {};
const range = profile?.cexFlows?.range || {};

With:

const summary = profile?.summary || {};
const range = profile?.range || {};

2. Replace DailyFlowTable call:

<DailyFlowTable items={profile.cexFlows?.items || []} />

With:

<DailyFlowTable items={profile.dailyFlows || []} />

3. TokenFlowDiagnostics still works, but now pass a compatible wrapper:

<TokenFlowDiagnostics diagnostics={{
  summary: profile.summary,
  diagnostics: profile.diagnostics
}} />

instead of:

<TokenFlowDiagnostics diagnostics={profile.flowDiagnostics} />

4. In SummarySection, if it reads profile.cexFlows.summary, change to profile.summary.

Find:

const summary = profile.cexFlows?.summary || {};

Replace with:

const summary = profile.summary || {};

5. If token fallback reads profile.leaderboardItem, keep it, but it will usually be null.
Use profile.analysisProfile directly where possible.
